<h1>Edit Kitten</h1>

<%= render "form", kitten: @kitten %>

<p>
  <%= link_to "Back to All Kittens", kittens_path %> |
  <%= link_to "Delete", kitten_path(@kitten),
        method: :delete,
        data: { confirm: "Are you sure you want to delete this kitten?" } %>
</p>
